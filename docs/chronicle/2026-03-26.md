# 2026-03-26 编年记录

## 代号索引
- `opus-0326a` — 训练循环 session（数据修复 + LR 重训 + RunPod DeBERTa 全量训练）

---

### [01:30] Turn 1 `opus-0326a`
**用户**: 检查 GitHub aidetector 状态，pull 下来
**执行**: git fetch → 发现 6 个新 commit（sliding window、CJK fix、adversarial 扩展）→ git pull fast-forward，+315 行
**效果**: 本地同步到 5931986
**决策**: 无冲突，直接 fast-forward

### [01:40] Turn 2 `opus-0326a`
**用户**: 继续训练 aidetector
**执行**:
1. 派 Explore agent 全面分析训练基础设施（21 个工具调用，84s）
2. 发现关键问题：
   - dataset_v3.jsonl **严重不平衡**（human 32K vs AI 17K）
   - LR v2 只用 2000 样本训练
   - DeBERTa cross-domain AUROC 0.5-0.6（随机水平）
   - 本地 M4 无法跑 DeBERTa 全量训练

**决策**: A) 修数据 + 重训 LR → B) RunPod 4090 全量 DeBERTa

### [01:55] Turn 3 `opus-0326a`
**执行**: 创建 build_dataset_v4.py，从 v3 + v1 + augmented + RAID 合并去重 → 平衡 dataset_v4.jsonl (69,176 samples, 17,294 × 4 classes)
**效果**: 解决了 v3 的不平衡问题

### [02:00] Turn 4 `opus-0326a`
**执行**:
1. 修改 train_lr_v2.py 指向 v4 数据集
2. 启动 LR v2 重训（5000 samples, --recompute, 后台运行）
3. 创建 finetune_v4.py（4-class labels + balanced data）
4. 启动本地 DeBERTa fine-tune（MPS）→ **OOM at 3%**（18GB 不够）
5. 生成 69K adversarial 训练数据（14 种攻击类型）
6. 创建 benchmark_models.py（离线评估脚本）

### [02:05] Turn 5 `opus-0326a`
**用户**: 不用 Colab，用 RunPod 4090
**执行**:
1. 发现 ~/.runpod/config.toml 有 API key
2. 查余额 $22.55，4090 $0.34/hr
3. 创建 SECURE cloud pod（第一个 COMMUNITY pod 无 SSH）
4. SSH 连上，装依赖
5. 遇到依赖兼容问题链：sentencepiece → torch 版本 → torchvision → batch_size OOM
6. 最终配置：torch 2.6.0+cu124, batch_size=8, grad_accum=8
7. 训练稳定运行 1.14 it/s

### [02:42] Turn 6 `opus-0326a`
**LR v2 训练完成**:
- 5000 samples, 4991 valid (9 skipped), 3819s compute
- **Model C (16 features + scaler): 79.8% accuracy**
- Old v1 on same test set: 68.6% → **+11.2% improvement**
- 旧的 "92.1%" 是 2000 样本上的过拟合
- Top feature: `s_std` (surprisal std) coef -15.4 → human 文本 surprisal 变化大

### [03:00] Turn 7 `opus-0326a`
**RunPod DeBERTa 训练完成**:
- 69,176 samples, 3 epochs, 42.7 min, RTX 4090
- **97.6% 4-class accuracy, 99.7% binary accuracy**
- Epoch progression: 96.2% → 97.4% → 97.6%
- Sanity: AI 检测准确 (98-100%)，但 human casual/essay 仍被误判 (55-75% AI)
- 下载 704MB model → models/detector_v4/, 替换为默认
- RunPod 花费 **$0.78** ($22.55 → $21.77)

### [03:06] 当前状态
- **LR v2**: ✅ 79.8% (+11.2% vs old)
- **DeBERTa v4**: ✅ 97.6% 4-class, 已集成
- **RunPod**: ✅ Terminated, $0.78 spent
- **下一步**: commit + push
